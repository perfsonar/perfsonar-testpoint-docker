apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "perfsonar.fullname" . }}-test-connection"
  labels:
    {{- include "perfsonar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "perfsonar.fullname" }}']
  restartPolicy: Never
