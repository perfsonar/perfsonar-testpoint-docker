version: '3.8'
services:
  testpoint:
    image: perfsonar/testpoint:staging
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: "host"
    restart: on-failure
    volumes:
       - ./compose/psconfig:/etc/perfsonar/psconfig
